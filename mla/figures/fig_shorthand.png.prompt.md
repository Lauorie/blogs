Educational analogy illustration: MEETING MINUTES kept as COMPRESSED SHORTHAND, a metaphor for Multi-head Latent Attention (MLA) caching in a large language model.

Layout, left to right:
1. Left: a THICK stack of paper titled "full transcript = classic KV cache". Inside show 4 rows; each row has one teal card "K" and one light-purple card "V". Below the stack a red label "65,536 B per speaker". Draw a red cross-out mark over this stack.
2. Middle: an arrow from the stack to the notebook, passing through a small funnel box labeled "compress (W_down)".
3. Right of the funnel: a slim green notebook titled "shorthand minutes = latent cache". Inside show 4 rows; each row has one teal chip labeled "c - 512 dims" and one SMALL orange stamp labeled "t - 64" beside it. Below the notebook a green label "1,152 B per speaker (56.9x smaller)". Add a tiny gray footnote under the notebook: "orange stamp explained later (RoPE lane)".
4. Far right: from the notebook, ONE dashed arrow that FORKS into two heads, pointing at two cards stacked vertically: a teal card "K - full" on TOP and a light-purple card "V - full" BELOW it, with the label "can be rebuilt anytime (W_uk / W_uv)".
Bottom caption: "Write a 576-dim shorthand per speaker. The full transcript can always be rebuilt from it."

STYLE (shared across a figure series - follow exactly): Clean flat vector illustration for a technical blog, 16:9 landscape, pure white background, thin dark-gray rounded outlines, sans-serif labels, generous whitespace. Palette: latent/shorthand chips = teal; K cards = teal with darker border; V cards = light purple; timestamp stamps = warm orange; notebook/cache = light green with green border; crossed-out/wasteful = light red with red cross. All text ENGLISH only, few words, correctly spelled. Numbers must be EXACT as written: 65,536 B; 1,152 B; 512; 64; 576; 56.9x. No 3D, no gradients, no photos, no watermark.
