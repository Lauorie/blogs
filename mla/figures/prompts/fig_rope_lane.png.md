Educational analogy illustration: TIMESTAMPS travel in their own small side lane, a metaphor for decoupled RoPE in Multi-head Latent Attention.

Layout, left to right:
1. Left: one standing speaker icon (orange outline, flat style) holding TWO cards: a teal card labeled "content question" and a smaller orange card labeled "timing question".
2. Center: a slim green notebook titled "shorthand minutes: content only, no time". Inside a single column of 4 teal chips labeled "c1" "c2" "c3" "c4". The chips carry NO timestamps.
3. Right beside the notebook: a NARROW vertical side column with light-orange background titled "time lane", holding 4 small orange stamps labeled "t1" "t2" "t3" "t4", each aligned with the matching chip row.
4. Dashed arrow from the teal "content question" card to the notebook, labeled "score the contents". Dashed arrow from the orange "timing question" card, routed BELOW the notebook, into the time lane, labeled "score the stamps". The two arrows must not cross through any box.
5. Far right: a white rounded box labeled "add the two scores", receiving one arrow from the notebook side and one from the time lane.
Bottom caption: "Rotating stamps cannot be folded into the shorthand - so timing gets its own small lane."

STYLE (shared across a figure series - follow exactly): Clean flat vector illustration for a technical blog, 16:9 landscape, pure white background, thin dark-gray rounded outlines, sans-serif labels, generous whitespace. Palette: shorthand chips = teal; timing elements = warm orange; notebook/cache = light green with green border; people = neutral gray-blue. All text ENGLISH only, few words, correctly spelled. No 3D, no gradients, no photos, no watermark.

CORRECTIONS (must follow):
- The "add the two scores" box must receive exactly TWO incoming arrows from DIFFERENT sources: one originating at the green notebook's right or top edge (the content score), routed ABOVE the time lane without crossing any box; the other originating at the time lane's right edge (the timing score).
- The "score the contents" arrow still points from the content-question card into the notebook; the "score the stamps" arrow still routes below the notebook and ends at the time lane. These two arrows are inputs, not the ones feeding the add box.
