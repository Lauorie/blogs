Educational diagram showing what attention computes for the NEWEST token in a decoder-only transformer.
Layout, left to right:
1. A 5x5 lower-triangular score matrix labeled "Q K^T (causal)": grid of small squares, only the lower-left triangle filled, upper-right triangle empty. The BOTTOM ROW of the matrix is highlighted solid orange; all other rows are faded gray. Arrow pointing at the bottom row with label "row for the newest token".
2. To the right, an exploded view of that bottom row: one single orange vector box labeled "q5  (new token's query)" with multiplication signs against a vertical stack of 5 teal vector boxes labeled "k1" through "k5", then an arrow through a small box labeled "softmax", then weights applied to a vertical stack of 5 light-purple vector boxes labeled "v1" through "v5", summed into one gray output box labeled "h5".
3. A bracket grouping k1..k5 and v1..v5 with the label "needs ALL keys and ALL values", and a small tag near q5 saying "needs only ONE query".
Bottom caption: "The new token brings its own q - but attends over every k and v in the sequence".

STYLE (shared across a 5-figure series - follow exactly): Clean flat vector illustration for a technical blog, 16:9 landscape, pure white background, thin dark-gray rounded outlines, sans-serif labels, generous whitespace. Consistent palette: token/text boxes = light blue fill with blue border; Query vectors = warm orange; Key vectors = teal; Value vectors = light purple; cache/storage = light green fill with green border; wasted/redundant compute = light red fill with red border and diagonal hatching. Dashed dark-gray arrows for data flow. All text in ENGLISH only, as few words as possible, every word correctly spelled. No 3D, no gradients, no photos, no watermark, no decorative clutter.
